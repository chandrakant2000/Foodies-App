

                </div>
            </div>
        </div>



    
    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" integrity="sha384-9/reFTGAW83EW2RDu2S0VKaIzap3H66lZH81PoYlFhbGU+6BZp6G7niu735Sk7lN" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.min.js" integrity="sha384-w1Q4orYjBQndcko6MimVbzY0tgp4pWB4lZ7lr30WKz0vr/aWKhXdBNmNb5D92v7s" crossorigin="anonymous"></script>
    
    <script type="text/javascript">
        $(document).ready(function () { 
            var viewport = window.innerWidth;
            if(viewport <= 992){
                $('.removeMinHeight').removeClass('minHeight');
            } else {
                $('.removeMinHeight').addClass('minHeight');
            }
            
            if(viewport <= 576) {
                $('#addNewCategory').addClass('btn-sm');
                $('#removeCategory').addClass('btn-sm');
            } else {
                $('#addNewCategory').removeClass('btn-sm');
                $('#removeCategory').removeClass('btn-sm');
            }

            $("#addNewCategory").click(function () {
                $("#add-new-category").fadeToggle("slow", "linear");
            });
            $("#removeCategory").click(function () {
                $("#remove-category").fadeToggle("slow", "linear");
            });
        });

        $(function() {
            $('#new-category-form').on('submit', function(e) {
                if(this.checkValidity() == false) {
                    e.preventDefault();
                    e.stopPropagation();
                    $(this).addClass("was-validated");
                 }
            });
            $('#remove-category-form').on('submit', function(e) {
                if(this.checkValidity() == false) {
                    e.preventDefault();
                    e.stopPropagation();
                    $(this).addClass("was-validated");
                 }
            });
            $('[data-toggle="popover"]').popover({
                sanitize: false,
            });
        });
        
        $(window).resize(function () {
            var viewport = window.innerWidth;
            if(viewport <= 992){
                $('.removeMinHeight').removeClass('minHeight');
            } else {
                $('.removeMinHeight').addClass('minHeight');
            }
            if(viewport <= 576) {
                $('#addNewCategory').addClass('btn-sm');
                $('#removeCategory').addClass('btn-sm');
            } else {
                $('#addNewCategory').removeClass('btn-sm');
                $('#removeCategory').removeClass('btn-sm');
            }
        });

    </script>

  </body>
</html>

